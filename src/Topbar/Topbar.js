import { TitleContainer, TodoTitle } from './Topbar.style';

const Topbar = () => {
    return (
        <TitleContainer>
            <TodoTitle>TODO</TodoTitle>
        </TitleContainer>
    );
}

export default Topbar;